<% provide(:username, @user.username) %>
<% provide(:page, "home") %>
<div class="wrapper">
  <div class="hero-unit">
    <h1><%= gravatar_for(@user) %> <%= @user.username.capitalize %></h1>
  </div>
</div>
